<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 11+ 包可见性：允许列出全部已安装应用 -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- 官方放行方式：overrideLibrary 必须放在 <uses-sdk> 标签内（非 <application>！）——
         允许合并 minSdk 33 的 miuix-blur（运行时已由 isRuntimeShaderSupported gate 安全降级） -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <application
        android:name="com.bail.lspfrifa.LSPFRIFAApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Xposed 基础标识（兼容旧框架探测） -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="基于 Frida-GumJS 的动态调试与脚本注入工具" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <!-- 跨进程脚本提供者：目标 App 冷启动拉取初始脚本 / 注册 Binder 通道 -->
        <provider
            android:name="com.bail.lspfrifa.provider.ScriptConfigProvider"
            android:authorities="${applicationId}.config_provider"
            android:exported="true"
            android:multiprocess="false"
            tools:ignore="ExportedContentProvider" />

        <!-- 宿主管理界面 -->
        <activity
            android:name="com.bail.lspfrifa.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>